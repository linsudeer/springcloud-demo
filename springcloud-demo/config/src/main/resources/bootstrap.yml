spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native
  security:
    user:
      password: admin
  application:
    name: config

server:
  port: 8888
debug: false
